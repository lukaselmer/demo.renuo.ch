<% content_for :title, page.title %>
<%# TODO: render special template depending on content_type class %>
<% page.content_types.each do |ct| %>
    <%= render 'content_types/rich_text', record: ct %>
<% end %>
